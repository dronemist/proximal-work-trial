schema_version = "1.2"
artifacts = []

[task]
name = "worktrial/002-restaurant-ops-v7adv"
description = "Replicate a multi-page website design from reference screenshots. Graded with per-(page,viewport) structured metrics aggregated via harmonic mean × anticheat multiplier."
authors = []
keywords = ["design-to-code", "html-css", "phase-2", "multi-page", "multi-viewport"]

[metadata]

[verifier]
timeout_sec = 1200.0

[verifier.env]
ANTHROPIC_API_KEY = "${ANTHROPIC_API_KEY}"

[agent]
timeout_sec = 3600.0

[environment]
build_timeout_sec = 900.0
os = "linux"
cpus = 2
memory_mb = 4096
storage_mb = 10240
gpus = 0
allow_internet = true
mcp_servers = []

[environment.env]

[solution.env]
