schema_version = "1.2"
artifacts = []

[task]
name = "worktrial/002-lumen-multipage"
description = "Phase 2: replicate a 5-page SaaS website design at 3 viewports (mobile/tablet/desktop). Graded with per-(page,viewport) SSIM aggregated via per-page harmonic mean and cross-page mean × anticheat multiplier."
authors = []
keywords = ["design-to-code", "html-css", "phase-2", "multi-page", "multi-viewport"]

[metadata]

[verifier]
timeout_sec = 1200.0

[verifier.env]

[agent]
timeout_sec = 1800.0

[environment]
build_timeout_sec = 900.0
os = "linux"
cpus = 2
memory_mb = 4096
storage_mb = 10240
gpus = 0
allow_internet = true
mcp_servers = []

[environment.env]

[solution.env]
